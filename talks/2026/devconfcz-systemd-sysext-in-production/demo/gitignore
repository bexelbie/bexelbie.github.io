# Patterns to add to the repo root .gitignore when this directory is committed.
# Rename or incorporate this file — it is not read by git automatically.

# Secrets
devconf-sysext-demo/tailscale.env

# Generated by setup.sh — contain live IPs and join tokens
devconf-sysext-demo/join-info.env
devconf-sysext-demo/headlamp-token
devconf-sysext-demo/setup-background.log

# Large downloaded files — re-created by prep-infra.sh
devconf-sysext-demo/fcos-vhd-cache/
