server {
    listen 8080;
    root /data;
    autoindex on;

    location /payload/ {
        # Flatcar OS update payloads (flatcar_production_update.gz)
    }

    location /sysext/ {
        # sysext .raw files and SHA256SUMS
    }
}
