source 'https://rubygems.org'

group :jekyll_plugins do
  # These are from the _config.yml
  gem "jekyll-paginate"
  gem "jekyll-feed"
  # These are from errors when they aren't included
  gem "kramdown"
  gem "jekyll-watch"
end
