source 'https://rubygems.org'

group :jekyll_plugins do
  # These are from the _config.yml
  gem "jekyll-paginate"
  # These are supposed to be built-in
  gem "jekyll-feed"
  gem "kramdown"
  # This needed for my use case
  gem "jekyll-watch"
end
